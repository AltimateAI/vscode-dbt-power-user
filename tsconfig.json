{
  "compilerOptions": {
    "module": "commonjs",
    "target": "es6",
    // should be dist - for automation testing, both extension and tests should be compiled by tsc
    // so that we will be able to use inversify container to get component instances
    "outDir": "dist",
    "experimentalDecorators": true,
    "emitDecoratorMetadata": true,
    "forceConsistentCasingInFileNames": true,
    "importHelpers": true,
    "types": ["reflect-metadata", "node", "mocha"],
    "lib": ["es6", "es2020", "dom"],
    "sourceMap": true,
    "rootDir": "src",
    "strict": true /* enable all strict type-checking options */
    /* Additional Checks */
    // "noImplicitReturns": true, /* Report error when not all code paths in function return a value. */
    // "noFallthroughCasesInSwitch": true, /* Report errors for fallthrough cases in switch statement. */
    // "noUnusedParameters": true,  /* Report errors on unused parameters. */
  },
  "exclude": ["node_modules", ".vscode-test", "new_lineage_panel"]
}
